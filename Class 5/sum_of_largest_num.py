def sum(arr, n):
    maxsum = 0
    tempsum = 0
    m = len(arr)
    for i in range(0,m):
        if arr[0]>arr[i]:
            maxsum = arr[0]+sum
    tempsum = maxsum


arr = [23, 12, 9, 7 ,8 , 11, 4, 6, 56]
sum(arr, 5)